{
  "name": "meteorite-collection-shop-spacerocks",
  "version": "0.1.0",
  "private": true,
  "description": "A personal meteorite collection, specimen exchange, bookshop, and research portal.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node --check app.js && node --check cart.js && node --check checkout.js && node --check scripts/import-inventory.mjs && node --check scripts/sync-specimen-ledger.mjs && node --check scripts/export-coa-batch.mjs && npm run ledger:check && npm run coa:export -- --check && npm test",
    "coa:export": "node scripts/export-coa-batch.mjs",
    "import:inventory": "node scripts/import-inventory.mjs",
    "ledger:check": "node scripts/sync-specimen-ledger.mjs --check",
    "ledger:sync": "node scripts/sync-specimen-ledger.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
